name = "dibakar-ai-backend"
main = "server.ts"
compatibility_date = "2024-03-01"
compatibility_flags = [ "nodejs_compat" ]

pages_build_output_dir = "./dist"

[vars]
NODE_ENV = "production"
# Note: Sensitive keys should be added via Cloudflare Dashboard or 'wrangler secret'
